services:
  mysql:
    labels:
      com.ryframe.runtime-acceptance-owner: "${RYFRAME_V07_OWNERSHIP_TOKEN:?}"

  redis:
    labels:
      com.ryframe.runtime-acceptance-owner: "${RYFRAME_V07_OWNERSHIP_TOKEN:?}"

  rustfs:
    labels:
      com.ryframe.runtime-acceptance-owner: "${RYFRAME_V07_OWNERSHIP_TOKEN:?}"

networks:
  default:
    labels:
      com.ryframe.runtime-acceptance-owner: "${RYFRAME_V07_OWNERSHIP_TOKEN:?}"

volumes:
  redis-test-data:
    labels:
      com.ryframe.runtime-acceptance-owner: "${RYFRAME_V07_OWNERSHIP_TOKEN:?}"
