# 生产环境配置
# 敏感信息（密码、密钥）通过 APP_ 环境变量注入，不写入文件

[logger]
level = "info"
format = "json"

[redis]
mode = "required"
host = "127.0.0.1"
port = 6379
max_pool_size = 32

[proxy]
# 默认假设 Nginx 通过回环地址连接；容器网络部署时必须改为实际代理 CIDR。
trusted_cidrs = ["127.0.0.1/32", "::1/128"]
