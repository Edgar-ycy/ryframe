# 开发环境配置
# 仅覆盖与默认值不同的项

[app]
name = "ryframe"
version = "0.1.0"
host = "0.0.0.0"
port = 8080

[database]
# 开发环境输出 SQL 摘要日志
# off = 关闭 | summary = SQL+耗时+行数 | full = 含完整SQL
sql_log_level = "full"

[database.primary]
driver = "mysql"
host = "localhost"
port = 3306
database = "ryframe_config"
username = "root"
password = "123456"
max_connections = 10
min_connections = 1

# 从库连接（读写分离，可选，可配置多个）
[[database.replicas]]
driver = "mysql"
host = "localhost"
port = 3306
database = "ryframe_device"
username = "root"
password = "123456"
max_connections = 5
min_connections = 1

[redis]
host = "127.0.0.1"
port = 6379
password = "yecy"
database = 0

[cors]
allow_origins = ["http://localhost:5173", "http://localhost:3000"]

[logger]
level = "debug"